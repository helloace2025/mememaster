# Single Railway service: Next.js UI + FastAPI (root Dockerfile)
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Hit the Next.js UI (not API-only). Start period allows cold boot.
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
